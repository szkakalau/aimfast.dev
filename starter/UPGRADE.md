# 一行升级

复制下面这行，粘贴到目标项目的 Claude 对话中：

---

```
把这个项目的 UI 升级到专业水准。先读一遍项目现有样式，然后依次做：1) 标题全部改用 sans-serif 加粗（等宽字体只留代码和数据标签）；2) 卡片去掉实线边框换成多层阴影；3) 装饰性 emoji 换成 lucide-react 图标；4) Hero 区域加径向渐变光晕；5) 设计 token 统一收进一个 CSS 文件。每改完一步验证构建后提交。
```

---

就这一行。不需要前置准备，不需要复制额外文件。
